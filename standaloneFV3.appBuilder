# Standalone GSM
#
## NEMS Application Builder file

COMPONENTS=( FMS FV3 )

# DH* uncomment or remove this once Intel 18 is standard on Theia,
# then remove standaloneFV3_intel18.appBuilder, update
# entries in tests/rt*.conf to use standaloneFV3.appBuilder
# instead of standaloneFV3_intel18.appBuilder, and check
# logic in tests/appbuild.sh
#FV3_MAKEOPT="INTEL18=Y"
# *DH

# The modules.nems and configure.nems are selected by
# conf/before_appbuilder_file.mk.
